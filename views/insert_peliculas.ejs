<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/picnic">
    <%- include('partials/navbar.html') %>
    <style>

        body {
            background-color: #343a40; /* Color de fondo oscuro */
            color: #e11212; /* Color del texto */
            padding-top: 70px; /* Añadir espacio al principio */
        }

        h1 {
            text-align: center; /* Centrar el título */
            margin-top: 20px; /* Espacio superior */
        }

        form {
            width: 60%; /* Ancho del formulario */
            margin: 0 auto; /* Centrar el formulario */
        }

        label {
            display: block; /* Mostrar las etiquetas en bloques */
            margin-top: 10px; /* Espacio superior */
        }

        input[type="text"],
        input[type="submit"] {
            width: 100%; /* Ancho completo */
            padding: 8px; /* Añadir espacio interno */
            margin-top: 5px; /* Espacio superior */
            box-sizing: border-box; /* Incluir el padding en el ancho total */
        }

        input[type="submit"] {
            background-color: #007bff; /* Color de fondo del botón */
            color: white; /* Color del texto del botón */
            border: none; /* Quitar borde */
            cursor: pointer; /* Cambiar cursor al pasar por encima */
        }

        input[type="submit"]:hover {
            background-color: #0056b3; /* Color de fondo del botón al pasar el mouse */
        }

        input[type="text"] {
            background-color: white; /* Color de fondo blanco para los campos de texto */
            color: white; /* Color del texto en los campos de texto */
            border: 1px solid #ced4da; /* Añadir un borde a los campos de texto */
            border-radius: 4px; /* Agregar bordes redondeados a los campos de texto */
            padding: 8px; /* Añadir espacio interno */
            margin-top: 5px; /* Espacio superior */
            width: 100%; /* Ancho completo */
            box-sizing: border-box; /* Incluir el padding en el ancho total */
        }
    </style>
</head>

<body>

<%- include('partials/navbar.html') %>

<h1>Insertar Película</h1>
<li class="nav-item">
    <a class="nav-link btn btn-primary" href="/peliculas">Volver</a>
</li>

<form action="/peliculas/insert" method="POST">

    <label for="director">Director</label>
    <select name="director" id="director_id">
        <% directores.forEach(function (director) { %>
            <option value="<%= director._id %>"><%= director.nombre %></option>
        <% }); %>
    </select>


    <label for="nombre">Nombre</label>
    <input id="nombre" type="text" name="nombre">

    <label for="duracion">Duración</label>
    <input id="duracion" type="text" name="duracion">

    <label for="premios">Premios</label>
    <input id="premios" type="text" name="premios">

    <label for="imagen">Imagen</label>
    <input id="imagen" type="text" name="imagen">

    <input type="submit" value="Insertar">
</form>

<%- include('partials/footer.html') %>

</body>
    <script>
        document.getElementById('director_id').addEventListener('change', function (){
        document.getElementById('director_nombre').value = this.value;
    });
</script>

</script>

</html>
